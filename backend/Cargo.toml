[package]
name = "ems-backend"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# TODO: 添加实际依赖
# tokio = { version = "1", features = ["full"] }
# tokio-modbus = "0.7"
# rumqttc = "0.24"  # MQTT 客户端
